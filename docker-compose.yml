services:
  backend:
    build:
      context: .
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend-build:
    build:
      context: .
      target: frontend-build
    command: ["sh", "-lc", "echo 'frontend built'"]
